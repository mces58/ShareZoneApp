import { calculateGradientEndpoints } from './calculate-gradient-endpoints';
import {
  scaleByAspectRatio,
  scaleFontSize,
  scaleHeight,
  scaleProportionally,
  scaleWidth,
} from './dimensions';
import { downloadFile } from './download-file';
import { formatPhoneNumber } from './format-phone-number';
import { openGallery } from './image-picker';
import { loadFonts } from './load-fonts';
import { shadowEffect } from './shadow-effect';
import { stripHtmlTags } from './strip-html-tags';
import { useSwipeVertical } from './swipe';

export {
  calculateGradientEndpoints,
  downloadFile,
  formatPhoneNumber,
  loadFonts,
  openGallery,
  scaleByAspectRatio,
  scaleFontSize,
  scaleHeight,
  scaleProportionally,
  scaleWidth,
  shadowEffect,
  stripHtmlTags,
  useSwipeVertical,
};
